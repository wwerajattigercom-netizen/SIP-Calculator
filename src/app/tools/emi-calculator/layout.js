export const metadata = {
  title: "EMI Calculator",
  description: "Calculate your Equated Monthly Installment (EMI) for any loan with an interactive amortization schedule.",
  alternates: { canonical: 'https://stepupcalculator.com/tools/emi-calculator' },
};

export default function Layout({ children }) {
  return children;
}
