export const metadata = {
  title: 'DCA vs STP — Systematic Transfer Plan Explained',
  description: 'Learn the difference between DCA and STP (Systematic Transfer Plan). When should you use a lumpsum STP vs a monthly DCA?',
  keywords: ['sip vs stp', 'systematic transfer plan', 'stp mutual fund', 'how stp works', 'stp calculator '],
};

export default function Layout({ children }) {
  return children;
}
